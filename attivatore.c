#include "library.h"

int main(int argc, char* argv[]) {

    // ? come comunicare che bisogna fare una scissione? SEGNALE

    // semaforo che controlla n atomi da decidere (possono lavorare 3 atomi insieme)
    // in caso di riuscita execvesu atomo
    
    /**
     *  Ogni STEP_ATTIVATORE mandi 
     * un segnale ad atomo che controlla un semaforo per scindere
     * la chiave per accedere a una sezione critica del semaforo
     * 
     * Iteri n volte e ogni volta controllata la condizione mandi un segnale come sopra ad atomo
    */

}